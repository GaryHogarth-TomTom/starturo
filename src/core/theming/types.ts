import { Theme as ReactNavigationTheme } from '@react-navigation/native';
import { Theme as ReactNativePaperTheme } from 'react-native-paper/lib/typescript/types';

export type Theme = ReactNativePaperTheme & ReactNavigationTheme;
